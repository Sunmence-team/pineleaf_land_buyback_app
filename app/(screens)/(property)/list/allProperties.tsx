import { AppText } from '@/components/AppText'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const allProperties = () => {
  return (
    <SafeAreaView>
        <AppText>allProperties</AppText>
    </SafeAreaView>
  )
}

export default allProperties