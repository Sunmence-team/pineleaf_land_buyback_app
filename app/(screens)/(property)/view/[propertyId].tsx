import { AppText } from "@/components/AppText";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const propertyDetails = () => {
  return (
    <SafeAreaView>
      <AppText>propertyDetails</AppText>
    </SafeAreaView>
  );
};

export default propertyDetails;
